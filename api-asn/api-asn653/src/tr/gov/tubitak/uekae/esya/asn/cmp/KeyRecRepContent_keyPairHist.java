/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.0.0, Date: 18-Mar-2016.
 */
package tr.gov.tubitak.uekae.esya.asn.cmp;

import com.objsys.asn1j.runtime.*;

public class KeyRecRepContent_keyPairHist extends Asn1SeqOf {
   private static final long serialVersionUID = 55;
   public final static Asn1Tag TAG =
      new Asn1Tag (Asn1Tag.CTXT, Asn1Tag.CONS, 2);

   static {
      setKey (_cmpRtkey._rtkey);
   }

   public String getAsn1TypeName()  {
      return "SEQUENCE OF";
   }

   public CertifiedKeyPair[] elements;

   public KeyRecRepContent_keyPairHist () {
      elements = null;
   }

   /**
    * This constructor initializes the internal array to hold the 
    * given number of elements.  The element values must be manually 
    * populated.
    */
   public KeyRecRepContent_keyPairHist (int numRecords) {
      elements = new CertifiedKeyPair [numRecords];
   }

   /**
    * This constructor initializes the internal array to hold the 
    * given the array.  
    */
   public KeyRecRepContent_keyPairHist (CertifiedKeyPair[] elements_) {
      elements = elements_;
   }

   /** Returns the number of elements in the SEQUENCE OF. */
   public int getLength() {
      return elements.length;
   }

   public Asn1Type[] getElementValues() { return elements; }


   public void decode
      (Asn1BerDecodeBuffer buffer, boolean explicit, int implicitLength)
      throws Asn1Exception, java.io.IOException
   {
      int llen = (explicit) ?
         matchTag (buffer, TAG) : implicitLength;

      int llen2 = llen;
      llen = matchTag (buffer, Asn1Tag.SEQUENCE);

      // decode SEQUENCE OF or SET OF

      java.util.LinkedList<CertifiedKeyPair> llist =
         new java.util.LinkedList<CertifiedKeyPair>();
      Asn1BerDecodeContext _context =
          new Asn1BerDecodeContext (buffer, llen);
      CertifiedKeyPair element;
      int elemLen = 0;

      while (!_context.expired()) {
         element = new CertifiedKeyPair();
         element.decode (buffer, true, elemLen);
         llist.add (element);
      }

      elements = new CertifiedKeyPair [llist.size()];
      llist.toArray (elements);

      if (!(elements.length >= 1)) {
         throw new Asn1ConsVioException ("elements.length", elements.length);
      }

   }

   public int encode (Asn1BerEncodeBuffer buffer, boolean explicit)
      throws Asn1Exception
   {
      int _aal = 0, len;

      // encode SEQUENCE OF or SET OF

      if (!(elements.length >= 1)) {
         throw new Asn1ConsVioException ("elements.length", elements.length);
      }

      for (int i = elements.length - 1; i >= 0; i--) {
         len = elements[i].encode (buffer, true);
         _aal += len;
      }

      _aal += buffer.encodeTagAndLength (Asn1Tag.SEQUENCE, _aal);

      if (explicit) {
         _aal += buffer.encodeTagAndLength (TAG, _aal);
      }

      return (_aal);
   }

   public void print (java.io.PrintWriter _out, String _varName, int _level)
   {
      if (elements != null) {
         for (int i = 0; i < elements.length; i++) {
            String name = _varName + " [" + i + "]";
            elements[i].print (_out, name, _level);
         }
      }
   }

}
