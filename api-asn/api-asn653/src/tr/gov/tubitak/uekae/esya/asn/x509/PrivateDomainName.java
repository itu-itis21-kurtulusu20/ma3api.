/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.0.0, Date: 18-Mar-2016.
 */
package tr.gov.tubitak.uekae.esya.asn.x509;

import com.objsys.asn1j.runtime.*;

public class PrivateDomainName extends Asn1Choice {
   private static final long serialVersionUID = 55;
   static {
      setKey (_ExplicitRtkey._rtkey);
   }

   public String getAsn1TypeName()  {
      return "PrivateDomainName";
   }

   // Choice element identifier constants
   public final static byte _NUMERIC = 1;
   public final static byte _PRINTABLE = 2;

   public PrivateDomainName () {
      super();
   }

   public PrivateDomainName (byte choiceId_, Asn1Type element_) {
      super();
      setElement (choiceId_, element_);
   }

   public String getElemName () {
      switch (choiceID) {
      case _NUMERIC: return "numeric";
      case _PRINTABLE: return "printable";
      default: return "UNDEFINED";
      }
   }

   public void set_numeric (Asn1NumericString value) {
      setElement (_NUMERIC, value);
   }

   public void set_printable (Asn1PrintableString value) {
      setElement (_PRINTABLE, value);
   }

   public void decode
      (Asn1BerDecodeBuffer buffer, boolean explicit, int implicitLength)
      throws Asn1Exception, java.io.IOException
   {
      int llen = implicitLength;

      // decode CHOICE

      Asn1Tag tag = new Asn1Tag ();
      buffer.mark (8);
      int len = buffer.decodeTagAndLength (tag);
      final int choiceLen = len;

      int offset = buffer.getByteCount(), declen;

      if (tag.equals (Asn1Tag.UNIV, Asn1Tag.PRIM, 18))
      {
         buffer.reset();
         Asn1NumericString numeric;
         numeric = new Asn1NumericString();
         numeric.decode (buffer, true, len);
         if (!((numeric.getLength() >= 1 && numeric.getLength() <= 16))) {
            throw new Asn1ConsVioException ("numeric.getLength()", numeric.getLength());
         }

         setElement (_NUMERIC, numeric);
      }
      else if (tag.equals (Asn1Tag.UNIV, Asn1Tag.PRIM, 19))
      {
         buffer.reset();
         Asn1PrintableString printable;
         printable = new Asn1PrintableString();
         printable.decode (buffer, true, len);
         if (!((printable.getLength() >= 1 && printable.getLength() <= 16))) {
            throw new Asn1ConsVioException ("printable.getLength()", printable.getLength());
         }

         setElement (_PRINTABLE, printable);
      }
      else {
         throw new Asn1InvalidChoiceOptionException (buffer, tag);
      }

      declen = buffer.getByteCount() - offset;
      if (choiceLen != Asn1Status.INDEFLEN && choiceLen != declen)
         throw new Asn1InvalidLengthException();
   }

   public int encode (Asn1BerEncodeBuffer buffer, boolean explicit)
      throws Asn1Exception
   {
      int _aal = 0, len;
      switch (choiceID) {
      // encode numeric
      case _NUMERIC:
         Asn1NumericString numeric = (Asn1NumericString) getElement();
         if (!((numeric.getLength() >= 1 && numeric.getLength() <= 16))) {
            throw new Asn1ConsVioException ("numeric.getLength()", numeric.getLength());
         }

         len = numeric.encode (buffer, true);
         _aal += len;
         break;

      // encode printable
      case _PRINTABLE:
         Asn1PrintableString printable = (Asn1PrintableString) getElement();
         if (!((printable.getLength() >= 1 && printable.getLength() <= 16))) {
            throw new Asn1ConsVioException ("printable.getLength()", printable.getLength());
         }

         len = printable.encode (buffer, true);
         _aal += len;
         break;

      default:
         throw new Asn1InvalidChoiceOptionException();
      }

      return _aal;
   }

   public void print (java.io.PrintWriter _out, String _varName, int _level)
   {
      indent (_out, _level);
      _out.println (_varName + " {");
      if (element != null) {
         element.print (_out, getElemName(), _level+1);
      }
      indent (_out, _level);
      _out.println ("}");
   }

}
