/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.0.0, Date: 18-Mar-2016.
 */
package tr.gov.tubitak.uekae.esya.asn.x509;

import com.objsys.asn1j.runtime.*;

public class TeletexOrganizationalUnitNames extends Asn1SeqOf {
   private static final long serialVersionUID = 55;
   static {
      setKey (_ExplicitRtkey._rtkey);
   }

   public String getAsn1TypeName()  {
      return "TeletexOrganizationalUnitNames";
   }

   public Asn1T61String[] elements;

   public TeletexOrganizationalUnitNames () {
      elements = null;
   }

   /**
    * This constructor initializes the internal array to hold the 
    * given number of elements.  The element values must be manually 
    * populated.
    */
   public TeletexOrganizationalUnitNames (int numRecords) {
      elements = new Asn1T61String [numRecords];
   }

   /**
    * This constructor initializes the internal array to hold the 
    * given the array.  
    */
   public TeletexOrganizationalUnitNames (Asn1T61String[] elements_) {
      elements = elements_;
   }

   /** Returns the number of elements in the SEQUENCE OF. */
   public int getLength() {
      return elements.length;
   }

   public boolean equals( Object obj ) {
      if ( this == obj ) return true;
      if ( !(obj instanceof TeletexOrganizationalUnitNames) ) return false;
      TeletexOrganizationalUnitNames rhs = (TeletexOrganizationalUnitNames) obj;
      Asn1T61String[] rhsElements = rhs.elements;

      if (elements == null && rhsElements != null) return false;

      if(elements != null && rhsElements == null) return false;

      if(elements == null && rhsElements == null) return true;

      if(elements.length != rhsElements.length) return false;

      for(int i = 0; i< elements.length; i++) {
         if (elements[i] == null && rhsElements[i] != null) return false;
         if (rhsElements[i] == null && elements[i] != null) return false;
         if (rhsElements[i] == null && elements[i] == null) continue;

         if(!elements[i].equals(rhsElements[i])) return false;
      }
      return true;
   }

   public int hashCode() {
      int __code = 1;

      if (elements != null) {
         for (int i = 0; i < elements.length; i ++) {
            if (elements[i] != null)__code = 31*__code + elements[i].hashCode();
         }
      }

      return __code;
   }

   public Asn1Type[] getElementValues() { return elements; }


   public void decode
      (Asn1BerDecodeBuffer buffer, boolean explicit, int implicitLength)
      throws Asn1Exception, java.io.IOException
   {
      int llen = (explicit) ?
         matchTag (buffer, Asn1Tag.SEQUENCE) : implicitLength;

      // decode SEQUENCE OF or SET OF

      java.util.LinkedList<Asn1T61String> llist =
         new java.util.LinkedList<Asn1T61String>();
      Asn1BerDecodeContext _context =
          new Asn1BerDecodeContext (buffer, llen);
      Asn1T61String element;
      int elemLen = 0;

      while (!_context.expired()) {
         element = new Asn1T61String();
         element.decode (buffer, true, elemLen);
         if (!((element.getLength() >= 1 && element.getLength() <= 256))) {
            throw new Asn1ConsVioException ("element.getLength()", element.getLength());
         }

         llist.add (element);
      }

      elements = new Asn1T61String [llist.size()];
      llist.toArray (elements);

      if (!((elements.length >= 1 && elements.length <= 4))) {
         throw new Asn1ConsVioException ("elements.length", elements.length);
      }

   }

   public int encode (Asn1BerEncodeBuffer buffer, boolean explicit)
      throws Asn1Exception
   {
      int _aal = 0, len;

      // encode SEQUENCE OF or SET OF

      if (!((elements.length >= 1 && elements.length <= 4))) {
         throw new Asn1ConsVioException ("elements.length", elements.length);
      }

      for (int i = elements.length - 1; i >= 0; i--) {
         if (!((elements[i].getLength() >= 1 && elements[i].getLength() <= 256))) {
            throw new Asn1ConsVioException ("elements[i].getLength()", elements[i].getLength());
         }

         len = elements[i].encode (buffer, true);
         _aal += len;
      }

      if (explicit) {
         _aal += buffer.encodeTagAndLength (Asn1Tag.SEQUENCE, _aal);
      }

      return (_aal);
   }

   public void print (java.io.PrintWriter _out, String _varName, int _level)
   {
      if (elements != null) {
         for (int i = 0; i < elements.length; i++) {
            String name = _varName + " [" + i + "]";
            elements[i].print (_out, name, _level);
         }
      }
   }

}
