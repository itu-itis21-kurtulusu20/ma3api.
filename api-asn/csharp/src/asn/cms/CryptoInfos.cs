// 
// This file was generated by the Objective Systems ASN1C Compiler
// (http://www.obj-sys.com).  Version: 6.6.4, Date: 10-Oct-2013.
// 
using System;
using Com.Objsys.Asn1.Runtime;

namespace tr.gov.tubitak.uekae.esya.asn.cms {

   using Attribute = tr.gov.tubitak.uekae.esya.asn.x509.Attribute;

   public class CryptoInfos : Asn1Type {
      public Attribute[] elements;

      static CryptoInfos ()
      {
         Asn1Type.SetKey2 (_etsi101733Values._rtkey);
      }

      public CryptoInfos () : base()
      {
         elements = null;
      }

      /// <summary>
      /// This constructor initializes the internal array to hold the 
      /// given number of elements.  The element values must be manually 
      /// populated.
      /// </summary>
      public CryptoInfos (int numRecords) : base()
      {
         elements = new Attribute [numRecords];
      }

      /// <summary>
      /// This constructor initializes the internal array to hold the 
      /// given the array.  
      /// </summary>
      public CryptoInfos (Attribute[] elements_) : base()
      {
         elements = elements_;
      }

      /// <summary> Returns the number of elements in the sequence. </summary>
      public int getLength() {
         return elements.Length;
      }

      public override void Decode
         (Asn1BerDecodeBuffer buffer, bool explicitTagging, int implicitLength)
      {
         int llen = (explicitTagging) ?
            MatchTag (buffer, Asn1Tag.SEQUENCE) : implicitLength;

         // decode SEQUENCE OF or SET OF

         System.Collections.ArrayList llist = new System.Collections.ArrayList();
         Asn1BerDecodeContext _context =
             new Asn1BerDecodeContext (buffer, llen);
         Attribute element;
         int elemLen = 0;

         while (!_context.Expired()) {
            element = new Attribute();
            element.Decode (buffer, true, elemLen);
            llist.Add (element);
         }

         elements = new Attribute [llist.Count];
         Asn1Util.ToArray(llist, elements);

         if (!(elements.Length >= 1)) {
            throw new Asn1ConsVioException ("elements.Length", elements.Length);
         }

      }

      public override int Encode (Asn1BerEncodeBuffer buffer, bool explicitTagging)
      {
         int _aal = 0, len;

         // encode SEQUENCE OF or SET OF

         if (!(elements.Length >= 1)) {
            throw new Asn1ConsVioException ("elements.Length", elements.Length);
         }

         for (int i = elements.Length - 1; i >= 0; i--) {
            len = elements[i].Encode (buffer, true);
            _aal += len;
         }

         if (explicitTagging) {
            _aal += buffer.EncodeTagAndLength (Asn1Tag.SEQUENCE, _aal);
         }

         return (_aal);
      }

      public override void Print (System.IO.TextWriter _out, 
                                  string _varName, int _level)
      {
         if (elements != null) {
            for (int i = 0; i < elements.Length; i++) {
               string name = _varName + "[" + i + "]";
               elements[i].Print (_out, name, _level);
            }
         }
      }
   }
}
