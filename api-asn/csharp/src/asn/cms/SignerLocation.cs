// 
// This file was generated by the Objective Systems ASN1C Compiler
// (http://www.obj-sys.com).  Version: 6.6.4, Date: 10-Oct-2013.
// 
using System;
using Com.Objsys.Asn1.Runtime;

namespace tr.gov.tubitak.uekae.esya.asn.cms {

   using DirectoryString = tr.gov.tubitak.uekae.esya.asn.x509.DirectoryString;

   public class SignerLocation : Asn1Type {
      public DirectoryString countryName;  // optional
      public DirectoryString localityName;  // optional
      public PostalAddress postalAdddress;  // optional

      static SignerLocation ()
      {
         Asn1Type.SetKey2 (_etsi101733Values._rtkey);
      }

      public SignerLocation () : base()
      {
         Init();
      }

      /// <summary>
      /// This constructor sets all elements to references to the 
      /// given objects
      /// </summary>
      public SignerLocation (
         DirectoryString countryName_,
         DirectoryString localityName_,
         PostalAddress postalAdddress_
      )
         : base ()
      {
         countryName = countryName_;
         localityName = localityName_;
         postalAdddress = postalAdddress_;
      }

      public void Init () {
         countryName = null;
         localityName = null;
         postalAdddress = null;
      }

      public override void Decode
         (Asn1BerDecodeBuffer buffer, bool explicitTagging, int implicitLength)
      {
         int llen = (explicitTagging) ?
            MatchTag (buffer, Asn1Tag.SEQUENCE) : implicitLength;

         Init ();

         // decode SEQUENCE

         Asn1BerDecodeContext _context =
            new Asn1BerDecodeContext (buffer, llen);

         IntHolder elemLen = new IntHolder();

         // decode countryName

         if (_context.MatchElemTag (Asn1Tag.CTXT, Asn1Tag.CONS, 0, elemLen, true)) {
            int offset = buffer.ByteCount;
            countryName = new DirectoryString();
            countryName.Decode (buffer, true, elemLen.mValue);
            int declen = buffer.ByteCount - offset;
            if (declen != elemLen.mValue && elemLen.mValue != Asn1Status.INDEFLEN)
               throw new Asn1InvalidLengthException();
         }

         // decode localityName

         if (_context.MatchElemTag (Asn1Tag.CTXT, Asn1Tag.CONS, 1, elemLen, true)) {
            int offset = buffer.ByteCount;
            localityName = new DirectoryString();
            localityName.Decode (buffer, true, elemLen.mValue);
            int declen = buffer.ByteCount - offset;
            if (declen != elemLen.mValue && elemLen.mValue != Asn1Status.INDEFLEN)
               throw new Asn1InvalidLengthException();
         }

         // decode postalAdddress

         if (_context.MatchElemTag (Asn1Tag.CTXT, Asn1Tag.CONS, 2, elemLen, true)) {
            int offset = buffer.ByteCount;
            postalAdddress = new PostalAddress();
            postalAdddress.Decode (buffer, true, elemLen.mValue);
            int declen = buffer.ByteCount - offset;
            if (declen != elemLen.mValue && elemLen.mValue != Asn1Status.INDEFLEN)
               throw new Asn1InvalidLengthException();
         }

      }

      public override int Encode (Asn1BerEncodeBuffer buffer, bool explicitTagging)
      {
         int _aal = 0, len;

         // encode postalAdddress

         if (postalAdddress != null) {
            len = postalAdddress.Encode (buffer, true);
            len += buffer.EncodeTagAndLength (Asn1Tag.CTXT, Asn1Tag.CONS, 2, len);
            _aal += len;
         }

         // encode localityName

         if (localityName != null) {
            len = localityName.Encode (buffer, true);
            len += buffer.EncodeTagAndLength (Asn1Tag.CTXT, Asn1Tag.CONS, 1, len);
            _aal += len;
         }

         // encode countryName

         if (countryName != null) {
            len = countryName.Encode (buffer, true);
            len += buffer.EncodeTagAndLength (Asn1Tag.CTXT, Asn1Tag.CONS, 0, len);
            _aal += len;
         }

         if (explicitTagging) {
            _aal += buffer.EncodeTagAndLength (Asn1Tag.SEQUENCE, _aal);
         }

         return (_aal);
      }

      public override void Print (System.IO.TextWriter _out, 
                                  string _varName, int _level)
      {
         Indent (_out, _level);
         _out.WriteLine (_varName + " {");
         if (countryName != null) countryName.Print (_out, "countryName", _level+1);
         if (localityName != null) localityName.Print (_out, "localityName", _level+1);
         if (postalAdddress != null) postalAdddress.Print (_out, "postalAdddress", _level+1);
         Indent (_out, _level);
         _out.WriteLine ("}");
      }
   }
}
