// 
// This file was generated by the Objective Systems ASN1C Compiler
// (http://www.obj-sys.com).  Version: 6.6.4, Date: 10-Oct-2013.
// 
using System;
using Com.Objsys.Asn1.Runtime;

namespace tr.gov.tubitak.uekae.esya.asn.cvc {

   public class NonSelfDescCVCwithHeader : Asn1Type {
      public new static readonly Asn1Tag _TAG = new Asn1Tag (Asn1Tag.APPL, Asn1Tag.CONS, 34);

      public NonSelfDescCVC cvc;
      public Asn1OctetString header;

      static NonSelfDescCVCwithHeader ()
      {
         Asn1Type.SetKey2 (_cvcValues._rtkey);
      }

      public NonSelfDescCVCwithHeader () : base()
      {
         Init();
      }

      /// <summary>
      /// This constructor sets all elements to references to the 
      /// given objects
      /// </summary>
      public NonSelfDescCVCwithHeader (
         NonSelfDescCVC cvc_,
         Asn1OctetString header_
      )
         : base ()
      {
         cvc = cvc_;
         header = header_;
      }

      /// <summary>
      /// This constructor allows primitive data to be passed for all 
      /// primitive elements.  It will create new object wrappers for 
      /// the primitive data and set other elements to references to 
      /// the given objects 
      /// </summary>
      public NonSelfDescCVCwithHeader (NonSelfDescCVC cvc_,
         byte[] header_
      )
         : base ()
      {
         cvc = cvc_;
         header = new Asn1OctetString (header_);
      }

      public void Init () {
         cvc = null;
         header = null;
      }

      public override void Decode
         (Asn1BerDecodeBuffer buffer, bool explicitTagging, int implicitLength)
      {
         int llen = (explicitTagging) ?
            MatchTag (buffer, _TAG) : implicitLength;

         Init ();

         // decode SEQUENCE

         Asn1BerDecodeContext _context =
            new Asn1BerDecodeContext (buffer, llen);

         IntHolder elemLen = new IntHolder();

         // decode cvc

         if (_context.MatchElemTag (Asn1Tag.APPL, Asn1Tag.CONS, 33, elemLen, false)) {
            cvc = new NonSelfDescCVC();
            cvc.Decode (buffer, true, elemLen.mValue);
         }
         else throw new Asn1MissingRequiredException (buffer);

         // decode header

         if (_context.MatchElemTag (Asn1Tag.APPL, Asn1Tag.PRIM, 13, elemLen, true)) {
            int offset = buffer.ByteCount;
            header = new Asn1OctetString();
            header.Decode (buffer, false, elemLen.mValue);
            int declen = buffer.ByteCount - offset;
            if (declen != elemLen.mValue && elemLen.mValue != Asn1Status.INDEFLEN)
               throw new Asn1InvalidLengthException();
         }
         else throw new Asn1MissingRequiredException (buffer);

         if (explicitTagging && llen == Asn1Status.INDEFLEN) {
            MatchTag (buffer, Asn1Tag.EOC);
         }
      }

      public override int Encode (Asn1BerEncodeBuffer buffer, bool explicitTagging)
      {
         int _aal = 0, len;

         // encode header

         len = header.Encode (buffer, false);
         len += buffer.EncodeTagAndLength (Asn1Tag.APPL, Asn1Tag.PRIM, 13, len);
         _aal += len;

         // encode cvc

         len = cvc.Encode (buffer, true);
         _aal += len;

         if (explicitTagging) {
            _aal += buffer.EncodeTagAndLength (_TAG, _aal);
         }

         return (_aal);
      }

      public override void Print (System.IO.TextWriter _out, 
                                  string _varName, int _level)
      {
         Indent (_out, _level);
         _out.WriteLine (_varName + " {");
         if (cvc != null) cvc.Print (_out, "cvc", _level+1);
         if (header != null) header.Print (_out, "header", _level+1);
         Indent (_out, _level);
         _out.WriteLine ("}");
      }
   }
}
