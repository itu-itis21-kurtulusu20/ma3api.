/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 6.6.4, Date: 19-Sep-2017.
 */
package tr.gov.tubitak.uekae.esya.asn.passport;

import com.objsys.asn1j.runtime.*;
import tr.gov.tubitak.uekae.esya.asn.x509.Certificate;

public class _SetOfCertificate extends Asn1Type {
   private static final long serialVersionUID = 55;
   static {
      setKey (_CscaMasterListRtkey._rtkey);
   }

   public Certificate[] elements;

   public _SetOfCertificate () {
      elements = null;
   }

   /**
    * This constructor initializes the internal array to hold the 
    * given number of elements.  The element values must be manually 
    * populated.
    */
   public _SetOfCertificate (int numRecords) {
      elements = new Certificate [numRecords];
   }

   /**
    * This constructor initializes the internal array to hold the 
    * given the array.  
    */
   public _SetOfCertificate (Certificate[] elements_) {
      elements = elements_;
   }

   public void decode
      (Asn1BerDecodeBuffer buffer, boolean explicit, int implicitLength)
      throws Asn1Exception, java.io.IOException
   {
      int llen = (explicit) ?
         matchTag (buffer, Asn1Tag.SET) : implicitLength;

      // decode SEQUENCE OF or SET OF

      java.util.LinkedList<Certificate> llist =
         new java.util.LinkedList<Certificate>();
      Asn1BerDecodeContext _context =
          new Asn1BerDecodeContext (buffer, llen);
      Certificate element;
      int elemLen = 0;
      int i = 0;

      while (!_context.expired()) {
         buffer.getContext().eventDispatcher.startElement("elements", i++);

         element = new Certificate();
         element.decode (buffer, true, elemLen);

         buffer.getContext().eventDispatcher.endElement("elements", i);
         llist.add (element);
      }

      elements = new Certificate [llist.size()];
      llist.toArray (elements);

   }

   public int encode (Asn1BerEncodeBuffer buffer, boolean explicit)
      throws Asn1Exception
   {
      int _aal = 0, len;

      // encode SEQUENCE OF or SET OF

      Asn1BerEncodeBuffer savedBuffer = buffer;
      buffer = new Asn1DerEncodeBuffer();
      Asn1OctetString[] elems = new Asn1OctetString [elements.length];

      for (int i = elements.length - 1; i >= 0; i--) {
         buffer.getContext().eventDispatcher.startElement("elements[" + i + "]", -1);

         len = elements[i].encode (buffer, true);
         _aal += len;

         buffer.getContext().eventDispatcher.endElement("elements[" + i + "]", -1);
         elems[i] = new Asn1OctetString (buffer.getMsgCopy());
         buffer.reset();
      }

      buffer = savedBuffer;
      java.util.Arrays.sort (elems);

      for (int i = elements.length - 1; i >= 0; i--) {
         elems[i].encode (buffer, false);
      }

      if (explicit) {
         _aal += buffer.encodeTagAndLength (Asn1Tag.SET, _aal);
      }

      return (_aal);
   }

   public void print (java.io.PrintStream _out, String _varName, int _level)
   {
      if (elements != null) {
         for (int i = 0; i < elements.length; i++) {
            String name = _varName + "[" + i + "]";
            elements[i].print (_out, name, _level);
         }
      }
   }
}
